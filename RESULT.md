# 数学动作与 Isaac 3D 验证结果

## 本轮做了什么

把门角离散为 0°、15°、30°、45°、60°。先用抽象门模型求解，再从 Isaac Sim 导出真实门铰链、把手轨迹、G1 pre-grasp 根位姿和关节顺序，重新求解与场景一致的五个姿态。G1 右掌在每个节点追踪把手；左右脚的位置和朝向保持初始值。每个姿态都以前一姿态 warm start，以避免关节解突然跳变。

优化残差由四部分组成：

1. 右掌与把手的位置误差；
2. 左右脚的 6D 固定误差；
3. 相对上一姿态的全身变化；
4. 相对初始自然站姿的偏离。

## Isaac 对齐精度

真实把手在 0°、15°、30°、45°、60° 的世界坐标已保存在 `outputs/scene_reference.json`。场景对齐后的误差为：

| 门角 | 右掌位置误差 | 双脚最大位置误差 |
|---:|---:|---:|
| 0° | 0.086 mm | 0.012 mm |
| 15° | 0.046 mm | 0.015 mm |
| 30° | 0.076 mm | 0.017 mm |
| 45° | 0.169 mm | 0.032 mm |
| 60° | 0.285 mm | 0.062 mm |

五个节点均收敛，且误差显著小于第一版抽象门模型的结果。

## 如何看图

关键帧图从左到右对应门逐渐打开。灰色柜体固定，蓝色门板绕铰链旋转，红点是把手目标，绿色 G1 姿态是优化结果。若右掌标记与红点重合且双脚不移动，说明几何闭链目标得到满足。

## 当前结论

这一步回答的是：不依赖学习策略，仅通过运动学闭链和最小变化原则，能否产生一条连续的全身开门姿态序列。

Isaac 3D 渲染由 Doorman 的 Isaac Sim 5.1 环境在 RTX 4090 上生成。最终推荐查看 `g1_fridge_isaac_static_side_keyframes.png`、`g1_fridge_isaac_static_side.gif` 和 `g1_fridge_isaac_static_side.mp4`。这些画面来自真实 G1/冰箱 USD 场景，不是 Matplotlib 骨架图或生成式图片。

随着门打开，骨盆、腰部和右臂连续调整。固定双脚方案在 60° 仍满足几何约束，但右肩余量减小；下一步应比较“继续固定双脚”和“迈一步”的动力学代价。

## 环境与故障结论

- 旧 `coordex` 环境的 CUDA 36 来自 Isaac Sim 5.0 / 旧 Warp 与服务器驱动接口组合，不是系统 `libcuda.so` 缺失；系统驱动 580.142 中存在 `cuDeviceGetUuid` 符号。
- Doorman 环境的 Isaac Sim 5.1 / Warp 1.10.1 在同一驱动上可正常启用 CUDA、Vulkan RTX 和 GPU PhysX，因此本项目改用该环境，不修改系统驱动。
- 动态创建或移动第二个离屏相机会遇到 Isaac 5.1 render product/资产加载状态不同步：`SimulationManager.assets_loading()` 长时间不清零，可能得到旧视角、空帧或初始化忙等。
- 最终解决方案是在环境创建前把侧视相机位姿写入 `CameraCfg.OffsetCfg`，并在每个姿态写入后调用 `sim.step(render=True)` 推进一次 RTX 帧。首尾帧像素验证和人工检查均通过，错误帧未作为结果保存。

它还不能回答动作在物理上是否省力或稳定。下一版需要在同一组关键姿态上加入：

- G1 质量和质心投影；
- 双脚受力与压力中心；
- 冰箱门阻力矩和右手切向力；
- 逐关节归一化力矩代价；
- 自碰撞及机器人与门板碰撞。

只有加入这些量之后，骨盆后移、躯干倾斜和是否迈步才会由静力平衡而不是单纯的几何可达性决定。
